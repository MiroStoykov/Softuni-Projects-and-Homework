function solve(input) {

    // let oddIndexes = input.filter((x, i) => i % 2 == 0);
    // console.log(oddIndexes.join(` `));

    // let arr=input.split(` `);
    // console.log(arr);

    input[1] += 20;
    console.log(input);
}

solve([10, 20, 30, 40, 50, 60]);